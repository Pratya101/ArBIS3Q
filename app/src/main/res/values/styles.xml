<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="HomeButton" parent="android:style/Widget.Button">
        <item name="android:textSize">@dimen/homeButtonFontSize</item>
        <item name="android:textColor">@color/colorHomeButtonText</item>
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:paddingBottom">@dimen/homeButtonPadding</item>
        <item name="android:layout_margin">@dimen/homeButtonMargin</item>

    </style>

    <style name="HomeButtonNamMol" parent="HomeButton">
        <item name="fontPath">fonts/Kit.ttf</item>

    </style>

    <style name="HomeButtonTishKid" parent="HomeButton">
        <item name="fontPath">fonts/Kit.ttf</item>

    </style>
</resources>
